# Tests

- [ ] Decide if base die is d120 or variant with explosions

## ⚙️ Mechanics

- **Standard test:** roll `d120` ≤ Stat (after modifiers).  
- **With modifier:** `Test(Stat + m)`  
- **Multiple:** `Test(k·Stat)`  
- **With threshold:** succeed only if roll `> T` and `≤ Stat`.

**Tags:** `#rules` `#tests`
